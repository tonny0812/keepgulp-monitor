server:
  port: 8080

spring:
  aop:
    auto: true
  jpa:
    database: MYSQL
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/monitor?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8&autoReconnect=true
    username: root
    password: root